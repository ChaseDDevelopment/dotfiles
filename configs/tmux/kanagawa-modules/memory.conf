# vim:set ft=tmux:
%hidden MODULE_NAME="memory"

set -ogq "@kanagawa_${MODULE_NAME}_icon" "󰍛 "
set -ogqF "@kanagawa_${MODULE_NAME}_color" "#{E:@thm_green}"
set -ogq "@kanagawa_${MODULE_NAME}_text" " #{l:#{ram_percentage}}"

source-file -q ~/.tmux/plugins/kanagawa-tmux/utils/status_module.conf
